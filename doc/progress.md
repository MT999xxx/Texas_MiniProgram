### [2026-03-18 14:00] - 排行榜添加会员级别显示
- [x] **需求**：在排行榜每个会员名次右侧显示其级别徽章（如"V5传奇钻石"）。
- [x] **修改**：
  - `index.wxml`：添加 `.rank-level-badge` 元素。
  - `index.wxss`：添加香槟金渐变徽章样式。
  - `loyalty.service.ts`：`levelName` 改为 `levelCode + level.name` 拼接格式。
- [x] **编译**：`tsc` 通过。

### [2026-03-03 00:42] - 金币充值重复加款严重 Bug 修复
- [x] **问题**：金币充值时余额被重复累加，导致会员 YuAnJ_ 余额膨胀至 12,001,280 金币。
- [x] **根因**：
  - `handlePaymentSuccess` 有两条并发触发路径（微信回调 + 前端轮询），无原子性保护，导致金币被双倍甚至多倍加到账户。
  - `handleRechargePaymentSuccess` 直接操作 `member.coins`，无幂等检查，无交易记录审计。
- [x] **修复**：
  - `handleWechatPayCallback`：使用原子 `UPDATE WHERE status != SUCCESS`，确保只有一条路径能触发后续逻辑。
  - `getPaymentStatus`：同样使用原子更新，防止与回调并发。
  - `handleRechargePaymentSuccess`：增加基于 `paymentOrderNo` 的 `CoinTransactionEntity` 幂等检查 + 创建审计记录。
  - `payment.module.ts`：注入 `CoinTransactionEntity`。
- [x] **编译**：`tsc` + `vite build` 均成功。
- **注意**：YuAnJ_ 余额需管理员手动修正。

### [2026-02-24 14:55] - 订单退款功能（含微信实际退款）
- [x] **需求**：管理员可以对客户错误下单的订单进行实际退款。
- [x] **后端**：
  - `CoinTransactionType` 新增 `REFUND` 枚举值。
  - `WechatPayService.refund()` 实现真实微信 V3 退款 API（`/v3/refund/domestic/refunds`），RSA-SHA256 签名，支持 mock 回退。
  - `OrdersService.refund()` 根据 `paymentMethod` 分发：微信支付调微信退款 API 实际退钱，金币支付退金币到会员账户。
  - `OrdersController` 新增 `POST /orders/:id/refund` 端点。
  - `OrdersModule` 注入 `PaymentModule`、`PaymentEntity`、`WechatPayService`。
- [x] **前端**：退款按钮对 `PAID` + `COMPLETED` 状态均显示。
- [x] **编译**：`vite build` 成功。

### [2026-02-24 14:45] - 后台订单详情显示修复
- [x] **问题**：订单详情中单价/小计显示 `¥undefined`，支付方式显示 `-`，数量显示不正确（12瓶显示为1）。
- [x] **根因**：
  - `OrderItemEntity` 未存储成交单价和规格类型，前端取 `menuItem.price` 在序列化后丢失。
  - `OrderEntity` 无 `paymentMethod` 字段，前端仅按状态推断且与后端枚举值不完全匹配。
  - 用户选择"一打"(dozen) 规格时 `quantity=1`（1打），但 `specType` 未持久化，无法还原瓶数。
- [x] **后端修复**：
  - `OrderItemEntity` 新增 `unitPrice`（成交单价）和 `specType`（规格类型）字段。
  - `OrderEntity` 新增 `paymentMethod` 字段，`markAsPaid()` 设为 `wechat_pay`，`payWithCoins()` 设为 `coins`。
  - `OrdersService.create()` 在创建订单项时保存 `unitPrice` 和 `specType`。
- [x] **前端修复**：
  - 单价：优先取 `unitPrice`，回退用 `amount/quantity` 反算，再回退取 `menuItem.price`。
  - 数量：根据 `specType` 显示如"1打 (12瓶)"或"1组 (6瓶)"。
  - 支付方式：优先读 `paymentMethod` 字段并翻译为中文，回退按状态推断。
  - 适配后端字段名 `orderNumber`/`notes` 的兼容映射。
- [x] **编译**：`vite build` 成功。
- **注意**：新字段均 `nullable`，已有旧订单数据不受影响（前端做了向下兼容）。部署后需 TypeORM 同步建表（synchronize: true）。

### [2026-02-10 10:55] - 后台管理系统手机端深度适配
- [x] **需求**：手机端比例不友好，部分区域看不全
- [x] **Dashboard**：hero-panel 按钮纵向排列、数据区自动换行、图表减高、排行榜精简间距
- [x] **Tables**：工具栏竖排、桌位操作按钮全宽排列
- [x] **Members**：搜索框全宽、表格行间距紧凑、按钮缩小
- [x] **Orders**：订单摘要自动换行、金额字号缩小、按钮紧凑
- [x] **Reservations**：工具栏换行、按钮精简、表格紧凑
- [x] **编译**：`npm run build` 成功，`dist` 目录已生成

### [2026-02-09 22:20] - 会员特权调整
- [x] **需求**：
  - 尊荣白银：赠送 7500 -> 1000 积分
  - 奢华黄金：赠送 20000 -> 7500 积分
  - 高贵铂金：8.5折 -> 9.5折，盲盒 -> 赠送 20000 积分
  - 巅峰钻石：7.5折 -> 8.5折，盲盒 -> 赠送 56000 积分
- [x] **小程序**：更新了 `member-level`、`recharge` 和 `member` 页面中所有的数值。

### [2026-02-06 14:25] - 预约规则礼仪动态配置
- [x] **需求**：后台管理系统需要能编辑"预约规则与礼仪"文案，小程序动态展示。
- [x] **后端**：创建 `settings` 模块，提供 `GET/PUT /settings/reservation-rules` API。
- [x] **后台**：在"系统设置"页添加新的"牌桌礼仪"Tab，支持规则条目的增删改。
- [x] **小程序**：`pages/table` 页面从 API 动态加载规则，wxml 使用 wx:for 渲染。
- [x] **数据表**：自动创建 `system_settings` 表（TypeORM synchronize）。

### [2026-02-05 20:40] - 后台会员搜索功能
- [x] **需求**：会员管理页面添加搜索功能，方便快速定位客户并调整积分/金币。
- [x] **实现**：使用 `Input.Search` 组件，支持按昵称或手机号实时过滤会员列表。
- [x] **样式**：搜索框与刷新按钮水平排列，金色搜索按钮符合整体风格。

### [2026-02-03 18:50] - 半打/一打价格计算修复
- [x] **问题**：购买半打（6瓶）或一打（12瓶）商品时，后端仍按单瓶价格×数量计算，而非使用配置的整打优惠价。
- [x] **根因**：后端 `OrdersService.create` 始终用 `menuItem.price * quantity` 计算，未读取 `halfDozenPrice` / `dozenPrice` 字段。
- [x] **修复**：
  - 后端 DTO (`create-order-item.dto.ts`) 新增 `SpecType` 枚举和 `specType` 可选字段。
  - 后端服务 (`orders.service.ts`) 根据 `specType` 选择正确的单价：single 用 `price`，half_dozen 用 `halfDozenPrice`，dozen 用 `dozenPrice`。
  - 前端 (`menu/index.js`) 提交订单时传递 `specType: item.specs?.package || 'single'`。
- [x] **兼容性**：老订单（无 `specType`）仍按单瓶价格计算，保持向后兼容。

### [2026-01-18 18:25] - 点餐金币支付功能
- [x] **后端API**：新增 `POST /orders/:id/pay-with-coins` 端点，扣除用户金币并标记订单已支付。
- [x] **前端UI**：结算时弹出支付方式选择（微信支付/金币支付/稍后支付），显示金币余额。
- [x] **余额校验**：金币不足时提示并引导用户去充值页面。

### [2026-01-18 16:45] - 登录持久化修复
- [x] **问题**：每次退出小程序后重新进入，都需要重新填写头像和昵称。
- [x] **根因**：`checkLogin()` 在 API 调用失败时无差别清除登录信息，导致网络波动也会导致用户登出。
- [x] **修复**：仅在收到 401/403 状态码时才清除登录信息，网络错误时保持使用本地缓存的用户信息。
- [x] **request.js 增强**：错误对象现在包含 `statusCode` 属性，便于上层判断错误类型。

### [2026-01-18 16:35] - 会员等级管理功能
- [x] **后端API**：新增 `PATCH /membership/members/:id/level` 端点，管理员可修改会员等级。
- [x] **前端UI**：会员管理页面的等级列可点击修改，弹出下拉选择框选择新等级。
- [x] **头像列**：将原"联系方式"列替换为"头像"列，使用 Avatar 组件显示。

### [2026-01-18 16:19] - Nginx 静态文件路由修复
- [x] **问题**：上传的头像图片返回 `Content-Type: text/html`，实际被 SPA 路由拦截。
- [x] **解决**：在 `/etc/nginx/nginx.conf` 中添加 `location /static/` 配置，将请求转发到后端 API 服务器。

### [2026-01-18 15:55] - 头像显示问题修复（增强）
- [x] **根因分析**：微信 `wx.chooseAvatar` 返回的是临时本地路径（`http://tmp/...`），该路径无法在服务器端或其他设备上访问。
- [x] **解决方案**：在登录时先将临时头像上传到服务器（`POST /uploads/avatar`），获取永久 HTTPS URL 后再发送给后端保存。
- [x] **前端增强**：新增 `isValidAvatarUrl()` 函数在显示头像时验证URL有效性，无效路径自动使用默认头像。
- [x] **错误处理**：会员页面和桌面页面添加 `binderror` 处理器，图片加载失败时回退到默认头像。
- **注意**：已存在的老用户需要重新登录才能更新头像。

### [2026-01-18 15:25] - 桌位自动重置与手动刷新功能
- [x] **自动重置定时任务**：每天凌晨0点（北京时间）自动将所有桌位状态重置为"空闲"，并取消过期预约。
- [x] **手动重置API**：新增 `POST /tables/reset-all` 端点供管理员触发。
- [x] **后台管理面板**：在桌位管理页面新增"重置桌位"按钮，可一键清除所有预约状态。

### [2026-01-18 14:15] - 排行榜显示修复与布局优化
- [x] **数据渲染修复**：纠正了 WXML 中因变量名不一致及条件判断逻辑错误导致的显示问题。
- [x] **布局深度优化**：大幅收缩顶部标题区域高度（内边距 80rpx -> 20rpx，字号 88rpx -> 52rpx），并**移除了 `redesign-bg` 导致的 `100vh` 布局遮挡**，实现排行榜秒速呈现，无需滑动。

## [Date/Time]:### [2026-01-13 17:05] - 充值系统 (Recharge) 奢华重构完成
- **[Done]** **充值中心界面升级**：
    - **账户英雄位**：顶部采用巨幕式毛玻璃账户卡片，积分余额以极致粗体渐变呈现。
    - **套餐矩阵**：充值套餐重构为高透玻璃质感网格，推荐项内嵌 `shimmer-effect` 流光动画。
    - **加密感支付层**：支付等待界面升级为黑色半透蒙版配合 `orb-loading` 动效，模拟银行级加密传输视觉。
- **[Done]** **充值记录精修**：
    - 采用“收据”式设计语言，每一条记录都配备了极细分割线与 CSS 箭头，强化专业收支感。
### [2026-01-13 17:20] - 礼遇与活动 (Coupons & Events) 奢华视觉闭环实现
- **[Done]** **礼遇券中心重构**：
    - **票券式设计**：采用左侧面值+右侧详情的经典票券结构，通过 CSS `radial-gradient` 实现物理缺口效果。
    - **分级展示**：V级专享标签与状态点阵（AVAILABLE/USED/EXPIRED）精准对齐“皇室金”设计规范。
- **[Done]** **活动中心升级**：
    - **沉浸式卡片**：活动封面采用暗调滤镜，信息层通过 `glass-panel` 半挂式叠层，营造高端俱乐部氛围。
    - **动态进度条**：参与人数进度重构为带有香槟金微光的极细流光条。
- **[Done]** **全二级页面对齐**：至此，充值、记录、券包、活动四大二级系统已全部完成“极致纯CSS”视觉重构。
- **[Next Step]** 最终整体走查，准备交付。
   - **全局样式 (`app.wxss`)**: 定义了极夜黑 (`#080808`)、香槟金 (`#D4AF37`) 设计规范。引入毛玻璃 (`glass-panel`)、金色流光边界 (`gold-border`) 等高级视觉组件。
   - **会员中心 (`pages/member`)**: 彻底重构。采用悬浮奢华会员卡布局，配合毛玻璃统计面板与金色线性功能按键。
   - **首页 (`pages/home`)**: 视觉升级。引入沉浸式 Hero 区域、悬浮品牌 Logo 霓虹动效，以及 **2列纵向排列** 的尊享功能卡片（点餐/预约）。
   - **桌面预约 (`pages/table`)**: 升级为椭圆布局牌桌视图，引入呼吸灯状态标识与尊享数据卡片。
   - **登录/授权 (`pages/login`)**: 全新设计。采用极夜黑渐变背景，金色辉光 Logo，以及更流畅的授权体验。
   - **排行榜 (`pages/ranking`)**: 配色统一。将原有的深红背景替换为极夜黑，排行榜项引入金色排行标识。
   - **全局导航 (`tab-bar`)**: 升级为毛玻璃透明质感，选中态字体采用香槟金色加粗高亮。
2. **紧急修复与优化**:
   - **代码修复**: 解决了 `member/index.wxss` 的语法错误以及 `catchtap` 方法缺失导致的报错。
   - **交互修复**: 极大提高了底部导航 (`tab-bar`) 的层级与点击响应性，解决了“点不动”的问题。
   - **布局优化**: 移除了旧版 AAA (TUSK) 样式的冲突，首页布局更加紧凑均衡。

### [Next Step]:
1. **用户操作**: 在开发者工具中全页面预览视觉效果。
2. **待办**: 后续若资源到位，可进一步替换为更高清、商务感更强的背景大图。

## [Date/Time]: 2026-01-05 11:25

### [Status]: In Progress

### [Changes]:
1. **小程序生产配置**：
   - 已将 `app.js` 中的 `apiBase` 从 `localhost:3000` 修改为 `http://47.109.44.38:3000`。
   - 已将 `cdnBase` 同步更新。
2. **后台管理系统打包**：
   - 已完成生产环境打包 (`npm run build`)，`dist` 目录已生成。
   - 打包耗时约 18 秒。

### [Next Step]:
1. **用户操作**：将 `dist` 目录上传到生产服务器。
2. **用户操作**：在服务器上使用 PM2 启动/重启服务端。
3. **用户操作**：在微信开发者工具中上传小程序并提交审核/发布体验版。

## [Date/Time]: 2026-01-05 10:45

### [Status]: Done

### [Changes]:
1. **桌位点餐功能**：
   - 在后台管理"桌位管理"页面为每张桌位卡片新增了"点餐"按钮。
   - 点击后会打开一个抽屉面板，显示菜单分类和菜品列表。
   - 支持将菜品添加到购物车，并可调整数量（增/减）。
   - 点击"提交订单"后，系统会自动生成一个关联该桌位的订单。
2. **API 扩展**：
   - 在 `orderApi` 中新增了 `create` 方法，用于前端调用 `POST /orders` 接口。
3. **首页背景模糊化**：
   - 为首页背景图添加了 `filter: blur(10rpx)` 模糊滤镜。
   - 同步添加了 `transform: scale(1.1)` 以消除模糊溢出导致的边缘问题，提升视觉质感。

## [Date/Time]: [2026-01-16 11:45]
[Status]: Done
[Changes]: 
1. **全链路 UI 细节体验优化实施完毕**：
   - **触感反馈 (Phase 1)**：在充值、存取积分、点餐确认等关键操作路径集成 `wx.vibrateShort`，提供物理级确认感。
   - **骨架屏系统 (Phase 1/2)**：开发了通用 `skeleton` 组件，适配名仕录、会员中心、点餐列表，彻底消除加载时的白屏闪烁。
   - **动效升级 (Phase 2/4)**：
       - **CountUp**：会员余额与积分支持数字滚动增长动画，强化财富获得感。
       - **弹窗动效**：重构了会员页多个交互弹窗的过渡曲线，采用 Layered scale + translateY 组合，呈现丝滑层级感。
   - **图形化空状态 (Phase 2)**：开发了 `empty` 组件，将单调的文字提示升级为“黑金”风格的 CSS 几何图形。
   - **后台管理系统增强 (Phase 3)**：为营收趋势图集成 `brush-x` 区域缩放与滚动条，优化了手机端布局兼容性，统一了表格 Hover 背景（#1a1a1a）。
[Next Step]: 最终项目走查，交付成果。

## [Date/Time]: [2026-01-15 14:10]
[Status]: Done
[Changes]: 登录、充值及排行榜页面完成全站视觉对齐。至此，全小程序核心页面均已同步至“极夜皇家金”2.0 沉浸式标准（CSS 动态背景 + 极致光效）。
[Next Step]: 开始 Phase 4 后台管理系统适配，或进行最终全局功能走查。

## [Date/Time]: 2026-01-05 10:30

### [Status]: Done

### [Changes]:
1. **会员页及其他区域背景黑化**：
   - 将会员中心页（member）的“会员卡片”（member-card）和“功能列表”（menu-list）背景由深灰色修改为纯黑色（#000000）。
   - 同时将金额充值弹窗（modal-content）的背景也修改为纯黑色，保持交互层级视觉统一。
2. **首页组件背景黑化**（已完成）：
   - 已完成首页欢迎框及操作面板的黑化处理。
3. **全局导航栏黑化**（已完成）：
   - 已完成底部 TabBar 的背景黑化处理。

### [Next Step]:
1. 用户在开发者工具中全面检查首页和会员页的纯黑背景效果。
2. 确认整体视觉的高级感和黑红配色方案是否符合预期。

## [Date/Time]: 2026-01-04 23:15

### [Status]: Done

### [Changes]:
1. **图标透明度修复**：
   - 修复了“点餐”和“预约”图标出现马赛克（棋盘格）背景的问题。
   - **根本原因**：JPG 格式不支持透明度。之前的修改将 `.png` 切换为了 `.jpg`，导致图片编辑器的透明背景预览（网格线）被直接固化在了 JPG 图片像素中。
   - **解决方案**：将图标文件后缀重新切回支持透明度的 `.png` 格式。
2. **首页布局对齐修复**（已完成）：
   - 修复了首页由于全局内边距导致的对齐偏移，并优化了操作面板项的宽度。
3. **小程序全量字体颜色变更**（已完成）：
   - 统一使用酒红配套色系，确保主体风格一致。

### [Next Step]:
1. 用户在微信开发者工具中预览首页和菜单页，确认图标背景是否恢复透明。
2. 继续进行全流程验证。
