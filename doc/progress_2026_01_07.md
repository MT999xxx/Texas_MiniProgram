# Progress Update - 2026-01-07

## [2026-01-07 02:00 - 03:00] 登录和支付问题修复

### Status: Done

### Changes:

#### 1. 修复登录循环问题 (JWT Token 验证失败)
- **文件**: `apps/server/src/auth/auth.module.ts`
- **问题**: `JwtModule.register()` 在模块加载时同步读取环境变量，此时 `dotenv.config()` 还未执行
- **修复**: 改为 `JwtModule.registerAsync()` 确保异步加载配置

#### 2. 修复支付需要 openid 问题
- **文件**: `apps/mp/utils/auth.js`
- **问题**: 登录时没有保存 `openid`，支付 API 无法获取
- **修复**: 在 `setAuth()` 和 `clearAuth()` 中增加 openid 的保存和清除

#### 3. 修复桌台预约报错
- **文件**: `apps/mp/pages/table/index.js`
- **问题**: Mock 数据中没有桌台 ID，导致预约时报 "桌台信息错误"
- **修复**: 为 Mock 数据添加 ID，改进 `loadTableData()` 失败时的 fallback

#### 4. 修复管理后台混合内容错误
- **文件**: `apps/admin/dist/*` (重新编译)
- **问题**: 旧编译版本中硬编码了 IP 地址 `http://47.109.44.38:3000`
- **修复**: 使用正确的 `.env.production` 重新编译，使用相对路径 `/api`

### Next Step:
- 上传新的 server dist 到服务器
- 上传新的 admin dist 到服务器
- 测试所有功能正常

### Files to Deploy:
1. `apps/server/dist/` → 服务器 `/var/www/texas-miniprogram/apps/server/dist/`
2. `apps/admin/dist/` → 服务器管理后台静态文件目录
