.empty-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 100rpx 40rpx;
  width: 100%;
  box-sizing: border-box;
}

.empty-icon-box {
  width: 160rpx;
  height: 160rpx;
  background: rgba(230, 198, 120, 0.05);
  border-radius: 50%;
  position: relative;
  margin-bottom: 40rpx;
  border: 1rpx solid rgba(230, 198, 120, 0.1);
}

.icon-inner {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 80rpx;
  height: 80rpx;
}

/* 基于 CSS 的黑金图标 - 盒子 (空列表) */
.type-box .icon-inner {
  border: 4rpx solid #E6C678;
  border-radius: 8rpx;
}
.type-box .icon-inner::after {
  content: "";
  position: absolute;
  top: 40%;
  left: 10%;
  width: 80%;
  height: 4rpx;
  background: #E6C678;
  box-shadow: 0 10rpx 0 #E6C678;
}

/* 基于 CSS 的黑金图标 - 列表 */
.type-list .icon-inner {
  width: 60rpx;
}
.type-list .icon-inner::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 4rpx;
  background: #E6C678;
  box-shadow: 0 20rpx 0 #E6C678, 0 40rpx 0 #E6C678;
}

.empty-text {
  font-size: 32rpx;
  color: #E6C678;
  font-weight: 500;
  margin-bottom: 16rpx;
}

.empty-desc {
  font-size: 24rpx;
  color: #888;
}
