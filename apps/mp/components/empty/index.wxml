<view class="empty-container">
  <view class="empty-icon-box type-{{type}}">
    <view class="icon-inner"></view>
  </view>
  <text class="empty-text">{{text}}</text>
  <text class="empty-desc" wx:if="{{description}}">{{description}}</text>
  <slot></slot>
</view>
