<view class="member-container">
  <!-- 头部会员卡 -->
  <view class="member-card">
    <view class="card-bg">
      <view class="member-info">
        <view class="avatar-section">
          <image class="avatar" src="{{userInfo.avatarUrl || '/assets/default-avatar.png'}}" mode="aspectFill" />
        </view>
        <view class="info-section">
          <view class="name-level">
            <text class="name">{{userInfo.nickname || '德州爱好者'}}</text>
            <text class="member-id">{{userInfo.city || '重庆'}}-{{userInfo.id || '6228'}}</text>
          </view>
        </view>
      </view>

      <!-- 会员等级卡片 -->
      <view class="level-card">
        <view class="level-badge">
          <image class="badge-icon" src="/assets/vip-badge.png" mode="aspectFit" />
          <view class="level-info">
            <text class="level-name">V{{memberLevel.level || 1}} {{memberLevel.name || '普通会员'}}</text>
          </view>
        </view>
        <view class="progress-section">
          <text class="progress-tip">(距离升下一级还差{{nextLevelGap}}成长值)</text>
          <text class="view-more" bindtap="viewBenefits">查看更多 ></text>
        </view>
      </view>

      <!-- 资产卡片 -->
      <view class="assets-card">
        <view class="asset-item" bindtap="navigateTo" data-url="/pages/recharge/index">
          <text class="asset-value">{{assets.balance || 0}}</text>
          <text class="asset-label">金币</text>
        </view>
        <view class="divider"></view>
        <view class="asset-item" bindtap="navigateTo" data-url="/pages/points/index">
          <text class="asset-value">{{assets.points || 0}}</text>
          <text class="asset-label">积分</text>
        </view>
        <view class="divider"></view>
        <view class="asset-item" bindtap="navigateTo" data-url="/pages/coupons/index">
          <text class="asset-value">{{assets.coupons || 0}}</text>
          <text class="asset-label">券</text>
        </view>
      </view>

      <button class="recharge-btn" bindtap="navigateTo" data-url="/pages/recharge/index">
        点击充值
      </button>
    </view>
  </view>

  <!-- 功能列表 -->
  <view class="function-list">
    <view class="function-item" bindtap="navigateTo" data-url="/pages/invite/index">
      <view class="function-icon">🎁</view>
      <text class="function-text">邀请有礼</text>
      <text class="arrow">></text>
    </view>

    <view class="function-item" bindtap="navigateTo" data-url="/pages/points/index">
      <view class="function-icon">💎</view>
      <text class="function-text">存积分</text>
      <text class="arrow">></text>
    </view>

    <view class="function-item" bindtap="navigateTo" data-url="/pages/order-list/index">
      <view class="function-icon">📋</view>
      <text class="function-text">订单列表</text>
      <text class="arrow">></text>
    </view>

    <view class="function-item" bindtap="navigateTo" data-url="/pages/reservation-list/index">
      <view class="function-icon">📅</view>
      <text class="function-text">我的预约</text>
      <text class="arrow">></text>
    </view>

    <view class="function-item" bindtap="navigateTo" data-url="/pages/settings/index">
      <view class="function-icon">⚙️</view>
      <text class="function-text">设置</text>
      <text class="arrow">></text>
    </view>
  </view>
</view>
